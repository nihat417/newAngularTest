import { NoteShorterPipe } from './note-shorter-pipe';

describe('NoteShorterPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteShorterPipe();
    expect(pipe).toBeTruthy();
  });
});
